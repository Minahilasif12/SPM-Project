services:
  - type: web
    name: market-trend-monitor-agent
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python agent.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
